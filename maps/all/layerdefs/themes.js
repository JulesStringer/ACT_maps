function declaredClimateEmergency(feature, resolution) {
    var style; // Return undefined to use default style rather than null
    var props = feature.getProperties();

    if (props.declared) {
    } else {
    }
}